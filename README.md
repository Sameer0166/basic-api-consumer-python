# Basic API Consumer using Python

This project demonstrates how to consume a public REST API using Python.
It fetches data from the GitHub API and displays selected information.

## Technologies Used
- Python
- requests library

## Features
- API request handling
- JSON response parsing

## How to Run
1. Install requests:
   pip install requests
2. Run:
   python api_fetch.py
